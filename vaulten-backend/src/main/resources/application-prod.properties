# ========================================
# Vaulten API - Production Profile
# ========================================
# This configuration will be completed in PHASE 4
# when integrating with Azure SQL and Key Vault

# Azure SQL Database (to be configured in Phase 4)
# spring.datasource.url=jdbc:sqlserver://${AZURE_SQL_SERVER}.database.windows.net:1433;database=${AZURE_SQL_DATABASE}
# spring.datasource.username=${AZURE_SQL_USERNAME}
# spring.datasource.password=${AZURE_SQL_PASSWORD}
# spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Hibernate / JPA
# spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
# spring.jpa.hibernate.ddl-auto=validate
# spring.jpa.show-sql=false

# JWT Configuration (from Azure Key Vault in Phase 4)
# jwt.secret=${AZURE_KEYVAULT_JWT_SECRET}
# jwt.expiration=86400000

# Logging
# logging.level.root=WARN
# logging.level.com.wilove.vaulten=INFO

# H2 Console (disabled in production)
# spring.h2.console.enabled=false
